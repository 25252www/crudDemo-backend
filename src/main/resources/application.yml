spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/crud_demo?useSSL=false&useUnicode=ture&characterEncoding=UTF-8&serverTimezone=GMT%2B8
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  config-location: classpath:/mybatis-config.xml
  type-aliases-package: com.moyu.pojo
  mapper-locations: classpath:mapper/*.xml
